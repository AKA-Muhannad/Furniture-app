import { StyleSheet } from "react-native";
import { SIZES } from "../constants";

export const ProductRowStyle = StyleSheet.create({
  productRowContainer: {
    marginTop: SIZES.medium,
    marginLeft: 12,
  },
});
